version: '1.0'
steps:
  build_image:
    type: build
    description: Building the image...
    image_name: advance512/prest

  perform_tests:
    image: advance512/prest
    working_directory: ${{main_clone}}
    description: Performing unit tests...
    commands:
      - go test
